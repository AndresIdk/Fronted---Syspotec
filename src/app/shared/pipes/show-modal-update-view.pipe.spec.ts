import { ShoModalUpdateViewPipe } from './show-modal-update-view.pipe';

describe('ShoModalUpdateViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ShoModalUpdateViewPipe();
    expect(pipe).toBeTruthy();
  });
});
