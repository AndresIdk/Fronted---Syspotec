import { UpdateHelperPipe } from './update-helper.pipe';

describe('UpdateHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
