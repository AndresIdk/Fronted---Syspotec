import { ShowModalPipe } from './show-modal.pipe';

describe('ShowModalPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowModalPipe();
    expect(pipe).toBeTruthy();
  });
});
