import { ModalConfirmationPipe } from './modal-confirmation.pipe';

describe('ModalConfirmationPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalConfirmationPipe();
    expect(pipe).toBeTruthy();
  });
});
